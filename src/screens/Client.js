import { View, Text } from 'react-native';
import React from 'react';
import CustomButton from "../components/CustomButton";

const Client = () => {
  return (
    <View style={{flex: 1}}>
      <Text>There are 666 walkers right now</Text>
    </View>
  );
};

export default Client;
