import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const HelpClient  = ({navigation}) => {
    return (
      <View>
        <Text>HelpWalker</Text>
      </View>
    )
  }

export default HelpClient

const styles = StyleSheet.create({
  input: {
    width: 100,
    height: 30,
  }
})