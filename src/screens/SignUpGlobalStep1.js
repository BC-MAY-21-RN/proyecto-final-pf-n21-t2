import React from 'react';
import {View, Text} from 'react-native';

const SignUpGlobalStep1 = ({navigation, route}) => {
  return (
    <View>
      <Text>route.params = {route.params}</Text>
    </View>
  );
};

export default SignUpGlobalStep1;
