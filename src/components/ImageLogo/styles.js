import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  image: {
    width: 260,
    height: 320,
    marginTop: -50
  },
  container: {
    padding: 50,
    alignItems: 'center'
  }
})

export default styles
